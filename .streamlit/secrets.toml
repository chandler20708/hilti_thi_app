HILTI_DATASET_PATH = "/mount/src/data/dataset2.xlsx"
HILTI_DISTRICT_PATH = "/mount/src/data/UK_postcode_districts.parquet"
HILTI_CASE_STUDY_PATH = "/mount/src/data/Hilti Case Study 2026.pdf"
# Map FastAPI (Render, etc.). Put API_BASE_URL *before* any ``[theme]`` block: in TOML, lines
# after ``[theme]`` belong to that table, so ``API_BASE_URL`` below ``[theme]`` becomes
# theme.API_BASE_URL (the app now resolves that too, but top-level is clearer).
API_BASE_URL = "https://hilti-thi-app.onrender.com"

# [theme]
# base = "light"
